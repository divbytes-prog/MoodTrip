# Optional: enables Google Places as an additional provider in the browser.
# Restrict this key by domain and API in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=

# Optional: enables anonymous shared feedback through the server-side API.
# Keep the service-role key server-side only.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
